#ifndef CORTEX_ENGINE_NORMALIZE_ENGINE_H
#define CORTEX_ENGINE_NORMALIZE_ENGINE_H

#include <string>

namespace cortex::engine {

// Declaration of the NormalizeEngine function
std::string NormalizeEngine(const std::string& engine);

} // namespace cortex::engine

#endif // CORTEX_ENGINE_NORMALIZE_ENGINE_H
