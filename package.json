{
  "name": "@janhq/cortex",
  "version": "1.0.0",
  "license": "AGPL-3.0",
  "scripts": {
    "preinstall": "npm pre-install script; platform specific (MacOS / Windows / Linux)",
    "build:core": "cd core && yarn && yarn build",
    "dev": "cd cortex-js && yarn start",
    "build": "cd cortex-js && yarn build",
    "build:extensions": "for dir in ./extensions/*/; do (cd \"$dir\" && yarn && yarn build); done"
  },
  "dependencies": {
    "express": "^4.17.1",
    "typeorm": "^0.2.37",
    "pg": "^8.7.1",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^14.14.33",
    "typescript": "^4F3.5"
  }
}
